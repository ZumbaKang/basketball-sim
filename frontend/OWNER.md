# Owner: Frontend Agent

**Owns:** slick UI, pages/components, client state, UX flows.

**Does not own:** DB schema, box-score math, trade/GM AI.

Consumes contracts from `../shared/`. See `../AGENTS.md`.
